# PostgreSQL - Advanced Study Plan (Summer 2026)
## Purpose and Objective of the Study
    By the end of this project, I should be able to:
        1. understands the different between the Postgres server and the pyscopg2 library 
        2. connect the postgres server with Python application/API with confidence and easy
        3. distinguish Linux/computer users from database users
        4. assign roles and privileges to database/SQL users - CREATEDB, INSERT, SELECT, DELETE, RBAC (CRUD)
        5. use both RAW SQL QUERIES, and ORM models from from SQLAlchemy
        6 solve joins operations 
        7. optimize queries performance with indexes
        8. backup and restore databases